@model IEnumerable<Integratie.Domain.Entities.Subjects.Organisation>
@{
    ViewBag.Title = "Organisaties";
}

<h2>Organisaties</h2>
<table class="table">
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
        </tr>
    }
</table>
